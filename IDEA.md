# Architecture

## Requirements summary

+ The polygon must be valid.
+ Someone node's attribute must be set to something.
+ Someone node's attribute must in the range.
+ Someone node can not exists.
+ Someone node's name must be prefix some string.
+ Someone node's name must be suffix some string.

## scripts

### scripts/core

The core base class.
